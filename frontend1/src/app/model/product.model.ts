export interface Product {
    id: number; // Cambia a 'number' para que sea compatible con JavaScript
    mark: string;
    price: number; // Puedes dejarlo opcional si no lo necesitas al mostrar la lista
  }